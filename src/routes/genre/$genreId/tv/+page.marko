<const/loadMore=(
  async (page: number) => {
    const params = new URLSearchParams({ page: String(page) });
    const url = `/genre/${$global.params.genreId}/tv/data?${params}`;
    const response = await fetch(url);
    return response.json();
  }
)/>

<infinite-media-grid
  collection=$global.collection
  name=`Tv Genre: ${$global.genre.name}`
  loadMore=loadMore
/>
